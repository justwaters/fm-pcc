#!/bin/sh
# Blocks the commit if any fast (mocked, offline) test fails. The slow
# real-model suite runs before releases instead: tests/run.sh slow.
set -e
cd "$(git rev-parse --show-toplevel)"
echo "pre-commit: running fast tests…"
tests/run.sh fast
